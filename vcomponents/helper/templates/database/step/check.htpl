 {if $do_database_setup} 
 <h2>Keine Datenbank konfiguriert</h2>
 
 <a class="btn btn-primary" href="/{$_document->getUrlPrefix()}database/configure">Mit Konfiguration der Datenbank fortfahren...</a>
 {else}
 <a class="btn btn-primary" href="/{$_document->getUrlPrefix()}database/models">Datenbank Layout bearbeiten</a>
 {/if}